this는 자기 자신 객체를 지정할 때 사용하는 키워드.
this. - 동일 클래스 내의 멤버(멤버변수, 메서드)를 참조
this()를 사용하면 생성자 내부에서 자신의 다른 생성자를 호출

this. 로 나의 멤버변수의 접근 가능
this() 로 나의 생성자의 접근이 가능

super는 한단계 위 부모클래스의 객체를 지정할 때 사용하는 키워드.
super. - 부모클래스의 멤버를 참조
super() - 부모클래스의 생성자를 호출하는데 사용
생성자의 첫 라인에는 반드시 this(), super()가 있어야 하는데 이를 기술하지 않으면 묵시적으로
super()가 삽입되어 부모클래스의 기본 생성자를 자동으로 호출

접근 제한자는 클래스와 멤버변수, 메서드, 생성자의 접근을 제어할 수 있는 제한자.
가장 앞에 붙음.
클래스에는 접근 제한자를 public과 default만 붙일 수 있음.

1. public: 같은 클래스, 같은 패키지, 다른 패키지를 막론하고 접근이 가능.
2. protected: 같은 클래스, 같은 패키지는 접근이 가능하지만 다른 패키지에 속해있는 클래스인 경우 상속관계
가 없으면 접근이 불가능.
3. default(접근 제한자를 붙이지 않는 형태): 같은 클래스, 같은 패키지에서만 접근이 가능하며 패키지가 다를
경우 접근이 불가능.
4. private: 같은 클래스 내부가 아니면 접근이 불가능.

은닉 - 사용자에게 상세한 내부 구현을 숨기고 필요한 부분만 보이게 하는 것.
setter메서드 - 변수의 값을 변경시키는데 사용되는 메서드 
getter메서드 - 변수의 값을 얻어오는데 사용하는 메서드

기본생성자 자동생성 Contral + Space + Enter 

getter, setter 자동생성 alt+shift+s
Source -> Generate Getters and Setters