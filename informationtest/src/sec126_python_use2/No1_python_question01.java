package sec126_python_use2;

public class No1_python_question01 {
	
	/*
	 * a = [1, 2, 3, 4, 5]
	 * x = 100;
	 * if x == 10:
	 * 	  a = list(map(lamda num : num + 10, a))
	 * elif x == 50:
	 * 	  a = list(map(lamda num : num + 50, a))
	 * elif x == 100:
	 * 	  a = list(map(lamda num : num + 100, a))
	 * print(a)
	 * 
	 * 결과값 - [101, 102, 103, 104, 105]
	 * 
	 */
	
	/* lamda식
	 * 
	 * lamda 변수명 : 수학식
	 * ex) def func(x):
	 * 	   	   return x * x - 3
	 *  -> func(x) = lamda x : x * x -3 
	 */
}
